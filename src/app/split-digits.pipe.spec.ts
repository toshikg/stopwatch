import { SplitDigitsPipe } from './split-digits.pipe';

describe('SplitDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
